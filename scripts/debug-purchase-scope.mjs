import pg from'pg';process.loadEnvFile?.('.env');const ref=new URL(process.env.SUPABASE_URL).hostname.split('.')[0],c=new pg.Client({host:'aws-0-us-east-1.pooler.supabase.com',port:6543,database:'postgres',user:`postgres.${ref}`,password:process.env.SUPABASE_DB_PASSWORD,ssl:{rejectUnauthorized:false}});await c.connect();try{console.table((await c.query('select p.product_id,p.item_code,p.product_usage,p.is_active,ps.subsidiary_id,ps.is_active linked from products p left join product_subsidiaries ps using(product_id) order by p.product_id')).rows);console.table((await c.query('select subsidiary_id,currency_id,is_primary from subsidiary_currencies where subsidiary_id=3')).rows);console.table((await c.query('select t.tax_code_id,t.code_name,t.subsidiary_id,ts.subsidiary_id linked from tax_codes t left join tax_code_subsidiaries ts using(tax_code_id)')).rows)}finally{await c.end()}
