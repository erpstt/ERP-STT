import pg from'pg';if(process.loadEnvFile)process.loadEnvFile('.env');const ref=new URL(process.env.SUPABASE_URL).hostname.split('.')[0],c=new pg.Client({host:'aws-0-us-east-1.pooler.supabase.com',port:6543,database:'postgres',user:`postgres.${ref}`,password:process.env.PGPASSWORD,ssl:{rejectUnauthorized:false}});await c.connect();try{const names=['department_subsidiaries','class_subsidiaries','financial_creditor_subsidiaries','related_company_subsidiaries','entity_subsidiaries','customer_currencies','supplier_currencies','chart_accounts','fiscal_periods','currencies','exchange_rates','departments','cost_centers','classes','financial_creditors','related_companies','customers','suppliers','employees'];const r=await c.query('select table_name from information_schema.tables where table_schema=$1 and table_name=any($2)',['public',names]);const found=new Set(r.rows.map(x=>x.table_name));console.log(names.map(name=>({name,exists:found.has(name)})))}finally{await c.end()}
