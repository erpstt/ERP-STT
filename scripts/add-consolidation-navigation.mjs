import{readFile,writeFile}from'node:fs/promises';const file='public/app.js';let s=await readFile(file,'utf8');s=s.replace('children: consolidationCatalogs.map(catalog => catalog.title)',"children: ['Consolidación financiera',...consolidationCatalogs.map(catalog => catalog.title)]");s=s.replace('navigateChild(module,child) { this.closeWorkspace();',"navigateChild(module,child) { this.closeWorkspace();if(module==='Consolidación'&&child==='Consolidación financiera'){this.currentModule=module;this.currentSection=child;this.openWorkspace('/consolidation.html');return;}");await writeFile(file,s,'utf8');
