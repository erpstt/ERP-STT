import{readFile,writeFile}from'node:fs/promises';const file='src/server.ts',source=await readFile(file,'utf8'),marker='    const consolidationRoute=';if(source.includes('const consolidationWorkflow=')){console.log('Rutas ya insertadas');process.exit()}const route="    const consolidationWorkflow=request.url?.match(/^\\/api\\/v1\\/accounting\\/consolidation(?:\\/(\\d+)\\/(run-translation|auto-eliminate|worksheet|publish))?$/);if(consolidationWorkflow){const authorization=request.headers.authorization;if(!authorization?.startsWith('Bearer '))return error(response,401,'Debe iniciar sesión para consolidar.');const id=consolidationWorkflow[1]?Number(consolidationWorkflow[1]):null,action=consolidationWorkflow[2];if(request.method==='GET'&&id===null)return json(response,200,await consolidationOptions(authorization));if(request.method==='POST'&&id===null)return json(response,201,await consolidationInit(authorization,await body(request)as Record<string,unknown>));if(id&&request.method==='POST'&&action==='run-translation')return json(response,200,await consolidationTranslate(authorization,id));if(id&&request.method==='POST'&&action==='auto-eliminate')return json(response,200,await consolidationProcess(authorization,id));if(id&&request.method==='GET'&&action==='worksheet')return json(response,200,await consolidationWorksheet(authorization,id));if(id&&request.method==='POST'&&action==='publish')return json(response,200,await consolidationPublish(authorization,id));}\n";const index=source.indexOf(marker);if(index<0)throw Error('No se encontró el punto de rutas');await writeFile(file,source.slice(0,index)+route+source.slice(index),'utf8');
