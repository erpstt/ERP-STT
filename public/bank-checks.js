try{await import('./bank-checks-main.js');const date=new Date(),lastDay=new Date(date.getFullYear(),date.getMonth()+1,0),offset=lastDay.getTimezoneOffset()*60000;document.getElementById('dateTo').value=new Date(lastDay-offset).toISOString().slice(0,10);document.getElementById('filters').requestSubmit()}catch(error){const target=document.getElementById('message');if(target)target.textContent=`No fue posible cargar las transacciones: ${error.message}`;throw error}
