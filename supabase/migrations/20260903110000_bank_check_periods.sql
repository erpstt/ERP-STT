create or replace function public.bank_check_periods() returns jsonb language sql stable security definer set search_path=public as $$select coalesce(jsonb_agg(to_jsonb(fp) order by fp.start_date),'[]'::jsonb) from fiscal_periods fp where fp.subsidiary_id=active_subsidiary_id()$$;revoke all on function public.bank_check_periods() from public,anon;grant execute on function public.bank_check_periods() to authenticated;notify pgrst,'reload schema';
