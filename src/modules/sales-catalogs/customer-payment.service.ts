import{getSupabaseConfig}from'../../core/database/supabase.client.js';async function rpc(a:string,n:string,p:Record<string,unknown>={}){const c=getSupabaseConfig();if(!c)throw Error('Supabase no está configurado.');const r=await fetch(new URL(`/rest/v1/rpc/${n}`,c.url),{method:'POST',headers:{apikey:c.anonKey,Authorization:a,'Content-Type':'application/json'},body:JSON.stringify(p)}),t=await r.text(),d=t?JSON.parse(t):null;if(!r.ok)throw Error(d?.message||'No fue posible procesar el cobro.');return d}export const customerPaymentOptions=(a:string)=>rpc(a,'customer_payment_options');export const customerPaymentReport=(a:string,p:Record<string,unknown>)=>rpc(a,'customer_payment_report',{p_filters:p});export const customerPaymentDetail=(a:string,id:number)=>rpc(a,'customer_payment_detail',{p_payment_id:id});export const saveCustomerPayment=(a:string,p:Record<string,unknown>,id?:number)=>rpc(a,'save_customer_payment',{p_payload:p,p_payment_id:id||null});export const deleteCustomerPayment=(a:string,id:number)=>rpc(a,'delete_customer_payment',{p_payment_id:id});
