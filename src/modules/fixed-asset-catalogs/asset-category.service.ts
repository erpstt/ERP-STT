import{getSupabaseConfig}from'../../core/database/supabase.client.js';async function rpc(a:string,n:string,p:Record<string,unknown>={}){const c=getSupabaseConfig();if(!c)throw Error('Supabase no está configurado.');const r=await fetch(new URL(`/rest/v1/rpc/${n}`,c.url),{method:'POST',headers:{apikey:c.anonKey,Authorization:a,'Content-Type':'application/json'},body:JSON.stringify(p)}),t=await r.text(),d=t?JSON.parse(t):null;if(!r.ok)throw Error(d?.message||'No fue posible procesar la categoría.');return d}export const assetCategoryOptions=(a:string)=>rpc(a,'asset_category_options');export const assetCategoryReport=(a:string,p:Record<string,unknown>)=>rpc(a,'asset_category_report',{p_filters:p});export const saveAssetCategory=(a:string,p:Record<string,unknown>,id?:number)=>rpc(a,'save_asset_category',{p_payload:p,p_category_id:id||null});export const deleteAssetCategory=(a:string,id:number)=>rpc(a,'delete_asset_category',{p_category_id:id});
