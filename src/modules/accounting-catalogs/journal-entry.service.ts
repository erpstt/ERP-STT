import { getSupabaseConfig } from '../../core/database/supabase.client.js';
async function call(authorization:string,functionName:string,body:Record<string,unknown>){const config=getSupabaseConfig();if(!config)throw new Error('Supabase no está configurado.');const response=await fetch(new URL(`/rest/v1/rpc/${functionName}`,config.url),{method:'POST',headers:{apikey:config.anonKey,Authorization:authorization,'Content-Type':'application/json'},body:JSON.stringify(body)});const raw=await response.text();const result:unknown=raw?JSON.parse(raw):null;if(!response.ok)throw new Error(typeof result==='object'&&result&&'message'in result?String(result.message):'No fue posible guardar el asiento.');return{journalId:Number(result)};}
export const createJournalEntry=(authorization:string,payload:Record<string,unknown>)=>call(authorization,'create_journal_entry',{payload});
export const updateJournalEntry=(authorization:string,journalId:number,payload:Record<string,unknown>)=>call(authorization,'update_journal_entry',{target_journal_id:journalId,payload});
