import{getSupabaseConfig}from'../../core/database/supabase.client.js';type FieldType='text'|'number';export interface BankingField{key:string;label:string;type:FieldType;required?:boolean;reference?:string;}export interface BankingCatalog{slug:string;table:string;title:string;description:string;primaryKey:string;fields:BankingField[];}
export const bankingCatalogs:BankingCatalog[]=[{slug:'banks',table:'banks',title:'Bancos',description:'Instituciones bancarias',primaryKey:'bank_id',fields:[{key:'bank_code',label:'Código',type:'text',required:true},{key:'bank_name',label:'Banco',type:'text',required:true},{key:'country_id',label:'País',type:'number',required:true,reference:'core/countries'}]},{slug:'bank-accounts',table:'bank_account',title:'Cuentas bancarias',description:'Cuentas bancarias de la empresa',primaryKey:'bank_account_id',fields:[{key:'account_number',label:'Número de cuenta',type:'text',required:true},{key:'bank_id',label:'Banco',type:'number',required:true,reference:'banking/banks'},{key:'subsidiary_id',label:'Subsidiaria',type:'number',required:true,reference:'organization/subsidiaries'},{key:'currency_id',label:'Moneda',type:'number',required:true,reference:'core/currencies'},{key:'account_id',label:'Cuenta contable',type:'number',required:true,reference:'accounting/chart-accounts'},{key:'balance',label:'Saldo',type:'number',required:true}]}];
export function getBankingCatalog(s:string){const c=bankingCatalogs.find(x=>x.slug===s);if(!c)throw new Error('El catálogo de Bancos no existe.');return c;}function sanitize(c:BankingCatalog,i:Record<string,unknown>){return Object.fromEntries(c.fields.map(f=>{const r=i[f.key];if(f.required&&(r===undefined||r===null||String(r).trim()===''))throw new Error(`${f.label} es obligatorio.`);if(f.type==='number')return[f.key,r===undefined||r===null||r===''?null:Number(r)];return[f.key,r===undefined||r===null||String(r).trim()===''?null:String(r).trim()];}));}async function request<T>(c:BankingCatalog,t:string,q='',i:RequestInit={}):Promise<T>{const g=getSupabaseConfig();if(!g)throw new Error('Supabase no está configurado.');const r=await fetch(new URL(`/rest/v1/${c.table}${q}`,g.url),{...i,headers:{apikey:g.anonKey,Authorization:t,'Content-Type':'application/json',...(i.headers??{})}});const x=r.status===204?'':await r.text();const p:unknown=x?JSON.parse(x):null;if(!r.ok){const m=typeof p==='object'&&p!==null&&'message'in p?String(p.message):'No fue posible completar la operación.';throw new Error(m);}return p as T;}
export const listBankingRows=(c:BankingCatalog,t:string)=>request<Record<string,unknown>[]>(c,t,`?select=*&order=${c.primaryKey}.asc`);export async function createBankingRow(c:BankingCatalog,t:string,i:Record<string,unknown>){const r=await request<Record<string,unknown>[]>(c,t,'',{method:'POST',headers:{Prefer:'return=representation'},body:JSON.stringify(sanitize(c,i))});return r[0];}export async function updateBankingRow(c:BankingCatalog,t:string,id:number,i:Record<string,unknown>){const r=await request<Record<string,unknown>[]>(c,t,`?${c.primaryKey}=eq.${id}`,{method:'PATCH',headers:{Prefer:'return=representation'},body:JSON.stringify(sanitize(c,i))});return r[0];}export const deleteBankingRow=(c:BankingCatalog,t:string,id:number)=>request<null>(c,t,`?${c.primaryKey}=eq.${id}`,{method:'DELETE'});
