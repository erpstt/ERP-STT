import{getSupabaseConfig}from'../../core/database/supabase.client.js';async function rpc(a:string,n:string,p:Record<string,unknown>={}){const c=getSupabaseConfig();if(!c)throw Error('Supabase no está configurado.');const r=await fetch(new URL(`/rest/v1/rpc/${n}`,c.url),{method:'POST',headers:{apikey:c.anonKey,Authorization:a,'Content-Type':'application/json'},body:JSON.stringify(p)}),t=await r.text(),d=t?JSON.parse(t):null;if(!r.ok)throw Error(d?.message||'No fue posible procesar Tesorería.');return d}export const treasuryWorkspace=(a:string,p:Record<string,unknown>)=>rpc(a,'treasury_workspace',{p_filters:p});export const saveTreasuryOperation=(a:string,p:Record<string,unknown>)=>rpc(a,'save_treasury_operation',{p_payload:p});export const saveTreasurySchedule=(a:string,p:Record<string,unknown>,id?:number)=>rpc(a,'save_treasury_schedule',{p_payload:p,p_schedule_id:id||null});export const deleteTreasurySchedule=(a:string,id:number)=>rpc(a,'delete_treasury_schedule',{p_schedule_id:id});
