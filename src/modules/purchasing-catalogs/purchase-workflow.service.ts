import{getSupabaseConfig}from'../../core/database/supabase.client.js';async function rpc(a:string,n:string,i:Record<string,unknown>={}){const c=getSupabaseConfig();if(!c)throw Error('Supabase no está configurado.');const r=await fetch(new URL(`/rest/v1/rpc/${n}`,c.url),{method:'POST',headers:{apikey:c.anonKey,Authorization:a,'Content-Type':'application/json'},body:JSON.stringify(i)}),t=await r.text(),x=t?JSON.parse(t):null;if(!r.ok)throw Error(x?.message||'No fue posible procesar el documento de compra.');return x}export const purchaseWorkflowOptions=(a:string)=>rpc(a,'purchase_workflow_options');export const purchaseDocuments=(a:string,p:Record<string,unknown>)=>rpc(a,'purchase_document_report',{p_filters:p});export const purchaseDocumentDetail=(a:string,id:number)=>rpc(a,'purchase_document_detail',{p_document_id:id});export const savePurchaseDocument=(a:string,p:Record<string,unknown>,id?:number)=>rpc(a,'save_purchase_document',{p_payload:p,p_document_id:id||null});export const deletePurchaseDocument=(a:string,id:number)=>rpc(a,'delete_purchase_document',{p_document_id:id});
